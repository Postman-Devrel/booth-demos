# Make MCPs Your Documentation Best Friend

> [Short talk](../../../templates/formats/short-talk.md) — target length **25–30 minutes**.
> Conference breakout, meetup main slot, or webinar. This README is the single source of
> truth. Read it top to bottom before you present; everything you need is here.
>
> **Status: draft.** The act plan below currently runs **~15 minutes** — it was first written
> for a booth slot. To fill a 25–30 minute slot, expand it against
> [templates/formats/short-talk.md](../../../templates/formats/short-talk.md): the missing beats
> are *why it happens* (the structural reason portals are human-only), *what it costs* (who
> maintains the four artifacts, and what generating them changes), and *objections* (the two
> questions you will be asked about MCP). The A/B in Act 7 stays as it is — it is the payoff.

This is a **deck plus a live A/B**. Twelve slides carry the argument, then slide 10 drops you
into the demo: the *same three APIs*, documented twice. Once the way we have always done it —
a beautiful, blue, human-first portal that ships on `localhost` with this repo — and once
generated by Fern, where the human page, the Markdown, the `llms.txt` and an MCP server all
come out of the same spec. You ask an agent one question against each and let the results
argue for you.

The question is chosen so it cannot be guessed: **"if I PATCH an appointment, what happens to
its slot?"** The answer is a genuine footgun in this API — a plain update emits
`appointment.cancelled`, and downstream `appointment-slots-service` reopens the slot. It is
documented in both places. Only one of them lets an agent find it.

---

## 1. Product summary

**Product:** Fern — one OpenAPI spec becomes the human docs site, per-page Markdown, an
`llms.txt` index, and an MCP server, generated together.

**Use case:** Show that a docs site can be excellent for people and useless to agents at the
same time, then show what generating the agent-facing surface from the spec actually buys you.

**The story (the narrative arc):**

> Documentation used to have one reader: a developer with your docs open in another tab. Now
> it has two, and the second one has to discover, authenticate, parse, and recover from
> errors on its own. Most portals were built for the first reader only — not out of
> negligence, but because the second reader did not exist when they were built. So here is
> one of those portals, and here is what happens when an agent tries to use it: an empty HTML
> shell, a 404 on `llms.txt`, no spec at a URL, and an answer it has to guess. Then the same
> three APIs through Fern: `llms.txt` lists all 24 endpoints, any page returns clean Markdown
> by adding `.md`, and an MCP server answers queries directly. Same question, same content,
> correct answer, three tool calls. The difference is not effort — it is that the agent
> surface is *generated from the spec* instead of curated by hand, so it cannot drift.

**CTA:**
- The "after" site — <https://devreliance-health.docs.buildwithfern.com>
- The MCP server — `https://devreliance-health.docs.buildwithfern.com/_mcp/server`
- Fern — <https://buildwithfern.com>
- Postman Discord — <https://discord.gg/postman>

**Total time: ~15 minutes.** Acts 1–6 are the deck (~9 min) and stand alone with no network.
Act 7 is the live A/B (~5 min) and is the reason to run this demo. Act 8 closes.

---

## 2. Pre-requisites

| Requirement | How to get it |
|---|---|
| A modern browser | Pre-installed. The deck and the portal are both plain HTML. |
| `python3` (3.8+) | Pre-installed on macOS. Serves the portal and builds the spec bundle. |
| PyYAML | `python3 -m pip install pyyaml`. `setup.sh` installs it for you if it is missing. |
| A terminal with a **large font** | Act 7 runs `curl` on stage. Bump the font size to ~18pt before you start. |
| An agent session — Claude Code, Cursor, or similar | Act 7 asks it the same question twice. Claude Code is what the talk track assumes. |
| Network *(Acts 7d–7g and 8 only)* | For the Fern site and the MCP server. The deck and the "before" half run fully offline. |
| **Nothing else** | The `healthcare-org` services do **not** need to be running. The portal's "Try it" panel is fully mocked and sends no requests. |

---

## 3. Setup

```bash
cd content/short-talks/mcp-docs-best-friend
./scripts/setup.sh
```

`setup.sh` will:

1. Confirm the deck exists at `presentation/index.html` and is a complete HTML file.
2. Confirm `python3` is present, and install PyYAML if it is missing.
3. **Rebuild the portal's spec bundle** from `openapi/*.yaml` into `site/assets/spec-bundle.js`.
4. **Assert the portal's anti-agent properties** — no `llms.txt`, no `openapi.yaml`, no
   `openapi.json`, no `sitemap.xml`, and an empty root div in `index.html`. These *are* the
   demo, so setup fails loudly if a well-meaning edit ever adds one.
5. Serve the portal at <http://localhost:4173> (override with `PORT=4180 ./scripts/setup.sh`).
6. Check the Fern site is reachable.
7. Open the deck and the portal in your browser.

### Authentication

None, anywhere. The deck authenticates against nothing, the portal's "Try it" panel never
sends a request, the Fern site is public, and its MCP server is unauthenticated. There is no
key to rotate and nothing to leak on stage.

### Pre-demo checklist

- [ ] Deck open and in **fullscreen** on slide 1 (press `f`, or Cmd+Ctrl+F on macOS)
- [ ] Portal open in a second tab at <http://localhost:4173>, scrolled to **API reference**
- [ ] Fern site open in a third tab: <https://devreliance-health.docs.buildwithfern.com>
- [ ] A terminal open in this folder, **large font**, ready for the Act 7 `curl` commands
- [ ] An agent session open (Claude Code) with a **clean context** — a warm one spoils Act 7c
- [ ] You have run the Act 7b curls once already, so nothing is cold on stage
- [ ] You know the four numbers cold: **1,402 bytes · 404 · 937 KB → 9.9 KB · 24 endpoints**
- [ ] Browser zoom set so text reads from 6 feet (`Cmd+=` / `Cmd+-`)

---

## 4. Talk track and click track

Eight acts, ~15 minutes. The deck's 12 slides, in order: **(1)** title · **(2)** speaker ·
**(3)** two readers · **(4)** what docs are for · **(5)** the agent-facing surface · **(6)**
how MCP works · **(7)** why it matters · **(8)** curating by hand · **(9)** one spec to rule
them all · **(10)** LIVE demo · **(11)** Discord · **(12)** thank you.

Deck navigation: **→ / space / click right** = next, **← / click left** = prev, **`f`** =
fullscreen, **Home** = back to slide 1.

Talk track is **verbatim** (blockquotes) — read it if you need to.

---

### Act 1: Open (1 min) — slides 1–2

**Show:** slide 1 — "Make MCPs Your Documentation Best Friend."

> "Quick show of hands — who here has an agent writing code against somebody else's API this
> week? Right. Then this talk is about the reader you did not design your documentation for."

**Do:** advance to slide 2.

> "I'm Anthony, Senior Developer Advocate at Postman. Sixteen years as a Java and JavaScript
> developer before this, and a core member of the JHipster project — so I have written my
> share of documentation nobody could use. That's the perspective I'm bringing."

---

### Act 2: Two readers (1.5 min) — slide 3

**Do:** advance to slide 3 — BEFORE / NOW.

> "Here's what changed. Before, your docs had exactly one reader: a human, skimming headings,
> hunting for the one paragraph they needed. The integration got written by that person. That
> was the whole model, and every docs tool we have was built for it."

**Show (payoff):** point at the NOW column.

> "Now there are two readers. A human still skims. But an agent has to do four things on its
> own — discover what exists, authenticate, parse the response, and recover when a call
> fails. Nobody is there to squint at the page for it. That's dual readership: the same
> content, read by two readers with nothing in common."

---

### Act 3: What documentation has to do (1 min) — slide 4

**Do:** advance to slide 4.

> "So what does documentation actually have to deliver? Three things. What the API *does* —
> endpoints and behaviour. What it *needs* — parameters, schemas, auth. And what it *shows* —
> worked examples and what happens when the call fails."

**Show (payoff):** the takeaway line.

> "Documentation is the interface between your API and whoever has to use it correctly. Not a
> description of your API for its own sake. And notice: nothing in those three bullets says
> 'human'. An agent needs all three too. It just cannot get them the same way."

---

### Act 4: The agent-facing surface, and MCP (3 min) — slides 5–7

**Do:** advance to slide 5.

> "So what do we build for that second reader? Three things. `llms.txt` — a plain-text index
> at the root of the site, so the agent knows what exists before it starts guessing URLs.
> Per-page Markdown — the same content as the HTML page with the navigation and the JS
> chrome stripped out, so it is cheap to parse. And an MCP server — a tool the agent calls
> directly instead of scraping and re-parsing pages itself."

**Show (payoff):** the takeaway.

> "Three formats, one purpose: give the agent a shortcut past the parts of the page it cannot
> use."

**Do:** advance to slide 6.

> "Quick primer on that third one, because MCP gets talked about a lot and explained rarely.
> Four pieces. The **host** is the app the human is in — Claude, an IDE, a custom agent. The
> **client** lives inside the host and manages exactly one connection to one server. The
> **server** is a program that exposes tools — for docs, that's something like `searchDocs`.
> And the **transport** carries JSON-RPC messages between them, usually over HTTP. That's it.
> It's a protocol for exposing tools, not a product."

**Do:** advance to slide 7.

> "Why this is good news specifically for documentation. First, your docs become *queryable*
> instead of scraped — the agent calls a tool instead of parsing raw HTML. Second, answers
> stay grounded: the response is scoped to what your docs actually say, not to what the model
> half-remembers about an API with a similar name. Third — and this is the one that matters
> at scale — it's one door, not one scraper per integration."

**Show (payoff):** the takeaway.

> "Which raises the obvious question. How do you keep all of that up to date? The docs, the
> `llms.txt`, the Markdown, *and* the MCP server."

---

### Act 5: Curating by hand (1 min) — slide 8

**Do:** advance to slide 8.

> "Imagine doing it by hand. You add a field to an endpoint. Step one, update the OpenAPI
> spec — the source everyone is supposed to trust. Step two, rewrite the affected doc pages
> and hope you didn't miss one. Step three, update `llms.txt` and the Markdown — if they
> exist, and if anyone still remembers those are separate files. Step four, patch the MCP
> server, re-implementing whatever changed, in a different repo, on a different sprint."

**Show (payoff):** the takeaway.

> "Four surfaces, four owners, four chances to forget. I'll say the quiet part: anything
> maintained by hand drifts. Not might — does. Usually within a quarter."

---

### Act 6: One spec to rule them all (1.5 min) — slide 9

**Do:** advance to slide 9.

> "So don't maintain them. Generate them. One OpenAPI spec plus your guides — the examples,
> the parameter definitions, the auth story — feeds all four outputs at once. The human docs
> site, same as always but always in sync. Per-page Markdown for every page. The `llms.txt`
> index. And the MCP server, which is the piece that lets an agent actually *use* the other
> three."

**Show (payoff):** the takeaway.

> "Nothing is copied by hand. Everything is built together, so it cannot drift. That's the
> claim — now let me show you the difference it makes."

---

### Act 7: The live A/B (5.5 min) — slide 10

**Do:** advance to slide 10 — the LIVE / Demo slide. Leave it up as your backdrop.

> "Three APIs from a hospital platform — appointments, appointment slots, prescriptions.
> Twenty-four endpoints. I've documented them twice. Same content, two builds. Let's meet the
> first one."

#### 7a — The portal that works beautifully (45 sec)

**Show:** the portal tab — <http://localhost:4173>.

**Do:** scroll through the overview, then into **API reference**. Expand
**`POST /api/appointments/`** and click **Try it out** → **Execute**.

**Show (payoff):** the 201 response comes back with the created appointment.

> "This is a good docs site. It really is — I'm not strawmanning it. Search, a sidebar, every
> endpoint with its parameters, its schemas, worked examples, a try-it panel that returns a
> real-looking 201. If you're a developer, this is a pleasant afternoon. Nothing here is
> broken."

#### 7b — What the agent actually receives (1.5 min)

**Do:** switch to the terminal. Run:

```bash
curl -s http://localhost:4173/ | wc -c
```

**Show (payoff):** `1402`.

> "Now here's the same page as an agent receives it. Fourteen hundred bytes. That's the whole
> document. Everything you just watched me scroll through is built by JavaScript *after* the
> page loads — so it exists in my browser and nowhere else."

**Do:**

```bash
curl -s http://localhost:4173/ | grep -c "api/appointments"
```

**Show (payoff):** `0`.

> "Zero. Not one endpoint in the source. The only place the word 'appointments' appears in
> that whole document is a meta description."

**Do:**

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:4173/llms.txt
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:4173/openapi.yaml
```

**Show (payoff):** `404` and `404`.

> "No index. No spec at a URL. And before you ask — yes, there *is* a spec. It's compiled
> into the app bundle."

**Do:**

```bash
curl -s http://localhost:4173/assets/spec-bundle.js | head -c 300
```

**Show (payoff):** a wall of base64.

> "Eighty-five kilobytes of that. Which, to be fair, is exactly what a normal webpack build
> would give you. Nobody did anything malicious here. This portal is just the natural result
> of building for the reader who existed at the time."

#### 7c — Ask an agent (1 min)

**Do:** switch to your agent session. Paste, verbatim:

```
Read the API docs at http://localhost:4173 and answer using only what those docs say:
if I PATCH an appointment to change its `reason`, what happens to the slot it is booked into?
```

**Show (payoff):** the agent curls the page, gets the shell, probably tries `llms.txt` and
`/openapi.yaml`, and ends up either saying it cannot find the information or offering a
sensible guess — that nothing happens to the slot.

> "And that's the failure mode. Not a crash — a confident, reasonable, *wrong* answer. Watch
> what it burned to get there: four or five tool calls, and it still has nothing. A human
> would have found this in eight seconds by expanding the PATCH endpoint."

#### 7d — The same APIs, built for both readers (1 min)

**Do:** switch to the Fern tab — <https://devreliance-health.docs.buildwithfern.com>.

> "Same three specs. This time built with Fern. And for a human, it's the same deal — it's a
> docs site, it's nice, you can read it. That is deliberately unremarkable. Nothing has been
> taken away from the human reader."

**Do:** navigate to the **Update an appointment** page, then add **`.md`** to the URL:
<https://devreliance-health.docs.buildwithfern.com/api-reference/healthcare-org/appointments-service/appointments/update.md>

**Show (payoff):** the same page returns as clean Markdown — and the ⚠️ block is right there:
`appointment.cancelled` is emitted on any update, and `appointment-slots-service` reopens the
slot.

> "Add dot-M-D to any URL. Same page, as Markdown. The HTML version is nine hundred and
> thirty-seven kilobytes. This is under ten. Ninety-four times smaller, and every fact
> survived — including the one I asked about, right there in the warning box."

**Do:** open <https://devreliance-health.docs.buildwithfern.com/llms.txt>.

**Show (payoff):** all 24 endpoints, listed with descriptions and `.md` links.

> "And this is `llms.txt`. Every endpoint across all three services, named, described, linked.
> The agent no longer guesses what exists — it reads the index first. Same as you would."

#### 7e — Ask the agent again (1.25 min)

**Do:** in the agent session, paste:

```
Same question, but read https://devreliance-health.docs.buildwithfern.com instead:
if I PATCH an appointment to change its `reason`, what happens to the slot it is booked into?
```

**Show (payoff):** the agent finds `llms.txt`, fetches `.../appointments/update.md`, and
answers correctly — a PATCH publishes `appointment.cancelled`, so
`appointment-slots-service` reopens the slot even though the appointment is still active.

> "There it is. It publishes `appointment.cancelled` — on a plain update — and downstream the
> slot gets reopened while the appointment is still live. That is a genuine footgun in this
> API, it is written down in both versions of the docs, and only one of them let the agent
> find it. Two tool calls instead of five, and the answer is right."

**Do (optional, +30 sec):** connect the MCP server and ask a third time.

```bash
claude mcp add --transport http myhealthcare-docs https://devreliance-health.docs.buildwithfern.com/_mcp/server
```

> "And the last step is to stop fetching altogether. That's the MCP server — one door, and
> now the agent queries the docs as a tool instead of downloading pages and re-reading them.
> Same content, same source, no scraping."

> **Fast version (skip 7d–7e):** if you're short on time, do 7a–7c, then just show
> `llms.txt` and one `.md` page and say the numbers. The contrast lands without the second
> agent run.

---

### Act 8: The close (45 sec) — slides 11–12

**Do:** advance to slide 11.

> "One more thing before I let you go — the Postman Community on Discord. Scan that, come ask
> questions, show us what you're building."

**Do:** advance to slide 12.

> "So: your docs have two readers now. You do not have to choose between them, and you should
> not maintain four artifacts by hand to serve them both. Write the spec, write the guides,
> and let the human site, the Markdown, the index, and the MCP server come out together —
> because that's the only version that can't drift. Slides and the docs MCP server are both
> linked from the event page. Grab me after, or find me on the Postman Discord."

---

## 5. Tear down / reset

```bash
./scripts/teardown.sh
```

| State the demo creates | What teardown does |
|---|---|
| A `python3 -m http.server` in front of `site/` | **Stops it** (by pid, then by port, so a hand-started one is caught too) |
| `.demo-state` (the server pid) | Removed |
| `/tmp/mcp-docs-portal.log` | Removed |
| `site/assets/spec-bundle.js` | **Kept** — setup rebuilds it every run. `--purge` deletes it. |

Nothing else exists to clean up: **no API calls are ever made** (the portal's "Try it" panel
is mocked), no credentials, no cloud resources, and nothing on the Fern side is touched.

**Between attendees:** `./scripts/teardown.sh && ./scripts/setup.sh`, or just reload the
portal tab and press `Home` in the deck. One thing you *must* do by hand: **clear your agent
session.** A warm context already knows the answer and Act 7c will not fail convincingly.

**Full reset between demo days:** `./scripts/teardown.sh --purge`, then `./scripts/setup.sh`.

---

## 6. Troubleshooting

| Issue | Fix |
|---|---|
| Port 4173 is already in use | `./scripts/teardown.sh` frees it, or run `PORT=4180 ./scripts/setup.sh` and use that URL everywhere in Act 7. |
| `setup.sh` fails on the spec bundle | It needs PyYAML: `python3 -m pip install pyyaml`. If pip is blocked, the checked-in `spec-bundle.js` still works — the build only matters if you edited `openapi/`. |
| Portal shows "Loading the API portal…" forever | `spec-bundle.js` failed to load. Hard-refresh (`Cmd+Shift+R`); if it persists, re-run `./scripts/build-spec-bundle.py`. |
| Portal is blank with no text at all | JavaScript is disabled in that browser profile. Which is, ironically, the whole point — but use a normal profile to present. |
| **The agent in Act 7c base64-decodes the bundle and answers correctly** | Congratulate it out loud, then land the real point: it spent five tool calls and 85 KB of context to extract what one `llms.txt` lookup gives for free — and a real minified webpack bundle would not have decoded that cleanly. The contrast in Act 7e still works. |
| The agent in Act 7c refuses to fetch `localhost` | Some tools block it. Paste `curl -s http://localhost:4173/` output into the session instead and ask the same question — the shell is the point, not who fetched it. |
| The agent in Act 7c answers correctly from prior knowledge | Its context is warm, or it recognised the platform. Start a fresh session and re-run. This is why the pre-demo checklist says clean context. |
| The agent in Act 7e still gets it wrong | Ask it to check `llms.txt` first, out loud: *"start from the llms.txt index."* That is itself the lesson — the index is what makes the rest findable. |
| No network — the Fern half is dead | Run Acts 1–7c, then say the numbers from memory: **937 KB of HTML becomes 9.9 KB of Markdown, ~94× smaller; `llms.txt` lists all 24 endpoints; the MCP server answers directly.** Slide 9 carries the argument on its own. |
| Deck won't open from `setup.sh` | Open `presentation/index.html` directly in a browser. |
| Deck fonts look wrong offline | The deck pulls Inter and JetBrains Mono from Google Fonts and falls back to system fonts without network. Layout is unaffected. |
| Arrow keys don't move the deck | Click on the slide first to give it focus. `f` fullscreens; `Home` returns to slide 1. |
| The Fern site's page layout changed | It's a live site. The `.md` trick, `llms.txt`, and `/_mcp/server` are stable; if a specific page moved, re-derive the URL from `llms.txt` on the spot — which is a nice accidental demo of `llms.txt` doing its job. |

---

## 7. Additional resources

| Resource | Link |
|---|---|
| The "before" portal (this repo) | `site/` — served at <http://localhost:4173> |
| The "after" site, built by Fern | <https://devreliance-health.docs.buildwithfern.com> |
| Its `llms.txt` index (Act 7d) | <https://devreliance-health.docs.buildwithfern.com/llms.txt> |
| The Act 7d Markdown page | <https://devreliance-health.docs.buildwithfern.com/api-reference/healthcare-org/appointments-service/appointments/update.md> |
| Its MCP server (Act 7e) | `https://devreliance-health.docs.buildwithfern.com/_mcp/server` |
| Source specs, copied into this demo | `openapi/appointments.openapi.yaml`, `openapi/appointment-slots.openapi.yaml`, `openapi/prescriptions.openapi.yaml` |
| Upstream services | <https://github.com/healthcare-org-app> |
| Model Context Protocol | <https://modelcontextprotocol.io> |
| Fern | <https://buildwithfern.com> |
| Postman Discord (slide 11) | <https://discord.gg/postman> |
| Companion demo — the full Fern product pitch | `../fern` |
| Companion demo — grade any docs site for agent-readiness, live | `../agent-ready-docs` |

---

## 8. How the "before" portal is built

Worth knowing, because an attendee will ask whether you rigged it.

`site/` is a hand-built single-page app: `index.html` is a 1.4 KB shell containing an empty
`<div id="root">`, and `assets/portal.js` constructs the entire portal — overview, platform
notes, every operation, every schema — at runtime from the three OpenAPI files. It renders
**27** operations against Fern's **24**: the specs declare three `PUT` aliases that Fern
collapses into their `PATCH` twin and the portal does not. Say "twenty-four endpoints" on
stage — that is the number in `llms.txt`, and the three aliases are not a real difference in
coverage. The look is
the API-explorer convention everyone recognises (method pills, collapsible operation rows,
parameter tables, a try-it panel), rebranded blue and carrying no third-party name or code.

Every property that makes it hostile to agents is a real property, and each one is common in
the wild:

| Property | How it's done | Why it's realistic |
|---|---|---|
| Content is invisible in the source | Client-rendered into an empty div | Every SPA docs portal behaves this way |
| No `llms.txt` | The file does not exist | It did not exist as a convention when most portals were built |
| No per-page Markdown | There are no per-page URLs at all — routing is `#`-fragments | Hash routing was standard for years |
| No fetchable spec | `scripts/build-spec-bundle.py` compiles `openapi/*.yaml` to base64 inside `assets/spec-bundle.js`; nothing is served as `.yaml` or `.json` | Bundlers inline data by default; publishing the raw spec is an extra, deliberate step |
| No MCP server | There isn't one | Same as almost every portal today |
| `robots.txt` disallows everything | `site/robots.txt` | Plenty of vendors block AI crawlers on purpose |

`setup.sh` asserts the first four every run, so the demo cannot quietly rot. The "Try it"
panel synthesises responses from the specs' own examples and never opens a socket — which is
why this demo needs nothing running and leaves nothing behind.
